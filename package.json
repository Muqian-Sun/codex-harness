{
  "name": "codex-harness",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.14.0",
  "engines": {
    "node": ">=24.14.0 <25",
    "pnpm": ">=10.14.0 <11"
  },
  "scripts": {
    "build": "tsc -b --force --pretty false && pnpm --filter @codex-harness/desktop build:renderer",
    "clean": "tsc -b --clean && node scripts/clean-desktop-renderer.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint . --max-warnings 0",
    "desktop:start": "pnpm build && pnpm --filter @codex-harness/desktop start",
    "schema:app-server": "node scripts/update-app-server-schema.mjs",
    "smoke:build": "node scripts/smoke-build.mjs",
    "test": "vitest run",
    "test:coverage:changed": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.typecheck.json --pretty false"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "24.13.3",
    "@vitest/coverage-v8": "4.1.10",
    "eslint": "10.8.0",
    "fast-check": "4.9.0",
    "globals": "17.8.0",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "typescript-eslint": "8.65.0",
    "vitest": "4.1.10"
  }
}
